apiVersion: v1
kind: Service
metadata:
  name: guestbook-app
spec:
  type: NodePort
  selector:
    app: guestbook
    tier: web
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30080
